import React from "react";

export default function Header(){
    return (
        <div>
            <nav className="header">
                <h2 className="header--title">Summify</h2>
            </nav>
        </div>
    )
}